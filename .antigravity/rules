当我输入指令'左右布局1:1'时，请自动将当前 HTML 中的左右布局容器（如 Flex 或 Grid 容器）的宽度比例调整为各占 50%，并确保响应式兼容。

当我输入指令'翻页适配'时，请自动将 pages 下的 HTML中 没有 slide-core.js 逻辑的文件，在其 body 标签底部自动加上 `<script src="../js/slide-core.js"></script>`。

当我输入指令'支持容器飞入动画'时，请自动将指定 HTML 文件中正文的容器元素添加飞入动画效果，能够支持键盘下键、右键播放下一个动画；键盘上键、左键播放上一个动画；同时注意不要影响翻页逻辑。